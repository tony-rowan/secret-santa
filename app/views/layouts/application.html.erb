<!DOCTYPE html>
<html>
  <head>
    <title>🎅🎁🎄 Secret Santa</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'tailwind', media: 'all' %>
    <%= javascript_tag 'application' %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="absolute -z-10 w-full h-full overflow-hidden">
      <% 100.times do %>
        <div class="w-1 h-1 rounded-full bg-white absolute animate-pulse"
          style="
            left: <%= rand(1..99) %>%;
            top: <%= rand(1..99) %>%;
            animation-delay: <%= rand(0.1..0.9) %>s;
            animation-duration: <%= rand(1.5..3.5) %>s;
            opacity: <%= rand(0..1) %>;
          ">
        </div>
      <% end%>
    </div>

    <div class="flex justify-center content-center items-center w-screen min-h-screen">
      <div class="max-w-screen-sm w-full p-8 md:px-0">
        <p class="text-4xl text-center mb-2">🎅🎁🎄</p>
        <h1 class="text-4xl text-center mb-4">Secret Santa</h1>

        <% if flash[:notice] %>
          <div class="my-8 bg-blue-400 rounded border-2 border-blue-600 p-4">
            <%= flash[:notice] %>
          </div>
        <% end %>

        <% if flash[:error] %>
          <div class="my-8 bg-red-400 rounded border-2 border-red-600 p-4">
            <%= flash[:error] %>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </div>
  </body>
</html>
