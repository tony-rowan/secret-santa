<nav class="py-8 px-4 bg-black/20">
  <div class="max-screen-lg container mx-auto flex flex-col sm:flex-row justify-between items-center gap-4 sm:gap-8">
    <%= link_to dashboard_path, class: "text-2xl sm:text-3xl md:text-4xl flex flex-col md:flex-row items-center sm:items-start gap-2" do %>
      <span>🎅🎁🎄</span>
      <span>Secret Santa</span>
    <% end %>

    <div class="flex flex-wrap gap-4">
      <% if Current.user %>
        <%= button_to "Sign Out", session_path, method: :delete, class: "md:text-lg rounded px-4 py-2 bg-indigo-800 hover:bg-indigo-900" %>
      <% end %>
    </ul>
  </div>
</nav>
